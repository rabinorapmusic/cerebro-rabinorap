pip install streamlit numpy
import streamlit as st

st.title("🎤 CEREBRO RABINO PRO")

tema = st.text_input(
    "Tema de la canción",
    "Dios me levantó del lodo"
)

if st.button("🔥 GENERAR ESTUDIO"):

    prompt = generar_prompt_estudio(
        tema=tema,
        artista="Rabino Rap"
    )

    st.text_area(
        "PROMPT GENERADO",
        prompt,
        height=600
    )
